import { ROLE_DEFAULTS } from "../../server/authorization/role-defaults";
export function RoleMatrix() { return <div style={{ overflowX: "auto" }}><table><caption className="sr-only">Role permissions</caption><thead><tr><th>Permission</th><th>Super admin</th><th>Admin</th><th>User</th></tr></thead><tbody>{[...new Set(Object.values(ROLE_DEFAULTS).flat())].sort().map((permission) => <tr key={permission}><td>{permission}</td>{(["super_admin", "admin", "user"] as const).map((role) => <td key={role}>{ROLE_DEFAULTS[role].includes(permission) ? "Allowed" : "Denied"}</td>)}</tr>)}</tbody></table></div>; }
